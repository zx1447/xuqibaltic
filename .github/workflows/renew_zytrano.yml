name: Zytrano Server Auto Renew

on:
  schedule:
    # 每天 04:00 UTC (北京时间 12:00) 自动校验是否达到了随机 2~10 天时间节点
    - cron: "0 4 * * *"
  workflow_dispatch:
    inputs:
      force:
        description: "是否强制立刻触发续期 (true/false)"
        required: false
        default: "false"

concurrency:
  group: zytrano-auto-renew
  cancel-in-progress: true

jobs:
  renew:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install and Start Cloudflare WARP (SOCKS5 Proxy)
        run: |
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ noble main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
          sudo apt-get update
          sudo apt-get install -y cloudflare-warp
          sudo warp-svc &
          sleep 3
          warp-cli --accept-tos registration new || true
          warp-cli --accept-tos mode proxy || true
          warp-cli --accept-tos proxy port 40001 || true
          warp-cli --accept-tos connect || true
          sleep 3
          echo "=== WARP PROXY CHECK ==="
          curl -x socks5h://127.0.0.1:40001 https://ipinfo.io/json || true

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install system deps (Chrome)
        run: |
          sudo apt-get update
          sudo apt-get install -y wget gnupg xvfb
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add - 2>/dev/null || true
          echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable || sudo apt-get install -y chromium-browser
          google-chrome --version || chromium-browser --version

      - name: Install Python deps
        run: pip install --upgrade pip seleniumbase requests "requests[socks]"

      - name: Install chromedriver
        run: python -m seleniumbase install chromedriver

      - name: Run Zytrano Renew
        env:
          ZYTRANO_USER: ${{ secrets.ZYTRANO_USER }}
          ZYTRANO_PASS: ${{ secrets.ZYTRANO_PASS }}
          ZYTRANO_COOKIE: ${{ secrets.ZYTRANO_COOKIE }}
          ZYTRANO_SERVER_ID: ${{ secrets.ZYTRANO_SERVER_ID }}
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          PROXY_SERVER: "socks5://127.0.0.1:40001"
          HEADLESS: "true"
          FORCE_RUN: ${{ github.event.inputs.force }}
        run: python renew_zytrano.py

      - name: Commit and push state change
        if: always()
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add zytrano_state.json || true
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update Zytrano random renew schedule state [skip ci]" && git push)
